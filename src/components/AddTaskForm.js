import React from 'react';

const AddTaskForm = () => (
    <input
              className="new-todo"
              placeholder="What needs to be done?"
              autoFocus
            />
);

export default AddTaskForm